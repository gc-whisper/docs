---
title: 'POST /auth/login'
description: 'Authenticate and obtain JWT tokens'
---

# Login

Authenticate with GameRamp API using your API credentials and receive JWT tokens for subsequent requests. The login response also includes initial player configurations.

<Card title="Endpoint" icon="server">
  ```
  POST https://api.gameramp.com/auth/login
  ```
</Card>

## Request

### Headers

<ParamField header="Content-Type" type="string" required>
  Must be `application/json`
</ParamField>

### Body Parameters

<ParamField body="apiKey" type="string" required>
  Your API Key provided by GameRamp
</ParamField>

<ParamField body="deviceId" type="string" required>
  Unique device identifier (e.g., device UDID, installation ID, or generated UUID)
</ParamField>

<ParamField body="timestamp" type="string" required>
  Current UTC timestamp in ISO 8601 format: `YYYY-MM-DDThh:mm:ssZ`
</ParamField>

<ParamField body="signature" type="string" required>
  HMAC-SHA256 signature encoded in Base64. See [Authentication](/api-reference/authentication) for generation details.
</ParamField>

### Example Request

<CodeGroup>
```bash cURL
curl -X POST 'https://api.gameramp.com/auth/login' \
  -H 'Content-Type: application/json' \
  -d '{
    "apiKey": "test_tenant_4Dqms3ygG9FVC",
    "deviceId": "device-123",
    "timestamp": "2025-07-15T19:44:26Z",
    "signature": "/Sn9msNKK4ifTEYrGC/GKpRptolLIyWi4jsfdn5S4g8="
  }'
```

```javascript Node.js
const crypto = require('crypto');
const axios = require('axios');

async function login(apiKey, deviceId, apiSecret) {
  const timestamp = new Date().toISOString().replace(/\.\d{3}/, '');
  const payload = `${apiKey}|${deviceId}|${timestamp}`;

  const signature = crypto
    .createHmac('sha256', apiSecret)
    .update(payload)
    .digest('base64');

  const response = await axios.post('https://api.gameramp.com/auth/login', {
    apiKey,
    deviceId,
    timestamp,
    signature
  });

  return response.data;
}
```

```python Python
import hmac
import hashlib
import base64
import requests
from datetime import datetime

def login(api_key, device_id, api_secret):
    timestamp = datetime.utcnow().strftime('%Y-%m-%dT%H:%M:%SZ')
    payload = f"{api_key}|{device_id}|{timestamp}"

    signature = base64.b64encode(
        hmac.new(
            api_secret.encode('utf-8'),
            payload.encode('utf-8'),
            hashlib.sha256
        ).digest()
    ).decode('utf-8')

    response = requests.post(
        'https://api.gameramp.com/auth/login',
        json={
            'apiKey': api_key,
            'deviceId': device_id,
            'timestamp': timestamp,
            'signature': signature
        }
    )

    return response.json()
```
</CodeGroup>

## Response

### Success Response (200 OK)

```json
{
  "accessToken": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refreshToken": "2b78c91e-5e14-4d75-a558-a45880d03233",
  "tokenType": "Bearer",
  "expiresIn": 900000,
  "userId": "test-device-456_20f6cab2",
  "deviceId": "test-device-456",
  "gameId": "default_game",
  "tenantId": "test_tenant_1752523421",
  "playerConfig": {
    "playerStatus": {
      "isPayer": {
        "value": true,
        "defaultValue": false,
        "expiredAt": null
      }
    },
    "storeConfig": {
      "activeSkus": [
        {
          "skuId": "starter_pack",
          "name": "Starter Pack",
          "description": "Perfect for new players",
          "actualPrice": 0.99,
          "offerPrice": 0.99,
          "currency": "USD",
          "assets": [],
          "active": true
        },
        {
          "skuId": "remove_ads",
          "name": "Remove Ads",
          "description": "Removes all advertisements",
          "actualPrice": 2.99,
          "offerPrice": 2.99,
          "currency": "USD",
          "assets": [],
          "active": true
        },
        {
          "skuId": "premium_currency_small",
          "name": "Premium Currency (Small)",
          "description": "100 premium coins",
          "actualPrice": 4.99,
          "offerPrice": 4.99,
          "currency": "USD",
          "assets": [],
          "active": true
        }
      ],
      "lastUpdatedTimestamp": 0,
      "userSpecificPromoId": null,
      "fallbackConfig": null
    }
  }
}
```

### Response Fields

<ResponseField name="accessToken" type="string">
  JWT access token for authenticating API requests
</ResponseField>

<ResponseField name="refreshToken" type="string">
  Token for refreshing the access token when it expires
</ResponseField>

<ResponseField name="tokenType" type="string">
  Always "Bearer"
</ResponseField>

<ResponseField name="expiresIn" type="number">
  Access token validity period in seconds
</ResponseField>

<ResponseField name="userId" type="string">
  Unique user identifier
</ResponseField>

<ResponseField name="deviceId" type="string">
  The device ID used in the request
</ResponseField>

<ResponseField name="gameId" type="string">
  Game identifier
</ResponseField>

<ResponseField name="tenantId" type="string">
  Tenant/Studio identifier
</ResponseField>

<ResponseField name="playerConfig" type="object">
  Initial player configuration including status and store settings

  <Expandable title="playerConfig properties">
    <ResponseField name="playerStatus" type="object">
      Player prediction status

      <ResponseField name="isPayer" type="object">
        Payer prediction

        <ResponseField name="value" type="boolean">
          Current prediction value
        </ResponseField>

        <ResponseField name="defaultValue" type="boolean">
          Default value if no prediction available
        </ResponseField>

        <ResponseField name="expiredAt" type="string | null">
          Expiration timestamp for the prediction
        </ResponseField>
      </ResponseField>
    </ResponseField>

    <ResponseField name="storeConfig" type="object">
      Store configuration

      <ResponseField name="activeSkus" type="array">
        Array of available products
      </ResponseField>

      <ResponseField name="lastUpdatedTimestamp" type="number">
        Last update timestamp
      </ResponseField>

      <ResponseField name="userSpecificPromoId" type="string | null">
        User-specific promotion ID
      </ResponseField>
    </ResponseField>
  </Expandable>
</ResponseField>

## Error Responses

### 401 Unauthorized

Invalid credentials or signature

```json
{
  "error": "Invalid authentication credentials",
  "status": "error"
}
```

### 400 Bad Request

Request timestamp outside acceptable window (±5 minutes)

```json
{
  "error": "Request timestamp is outside the acceptable time window",
  "status": "error"
}
```

## Security Notes

<Warning>
- API Secret must be kept secure and never exposed in client-side code
- Timestamps must be within 5 minutes of server time
- Each device should use a unique, persistent device ID
</Warning>

## Testing

Use these test credentials for development:

```json
{
  "apiKey": "test_tenant_4Dqms3ygG9FVC",
  "apiSecret": "b815ebac-9744-43db-b5e6-89f90620297e-Wjl8qlWrJn2r4OEB7UXJ_L8iYOLTR8p10t83Wi_79Q"
}
```

### Test Request

```bash
curl -X POST 'http://api.gameramp.com/auth/login' \
  -H 'Content-Type: application/json' \
  -d '{
    "apiKey": "test_tenant_4Dqms3ygG9FVC",
    "deviceId": "device-123",
    "timestamp": "2025-07-15T19:44:26Z",
    "signature": "/Sn9msNKK4ifTEYrGC/GKpRptolLIyWi4jsfdn5S4g8="
  }'
```

## Next Steps

After successful login:

<CardGroup cols={2}>
  <Card
    title="Use Access Token"
    icon="key"
    href="/api-reference/authentication#using-the-jwt-token"
  >
    Learn how to use the JWT token
  </Card>
  <Card
    title="Track Events"
    icon="chart-line"
    href="/api-reference/endpoints/events"
  >
    Start sending player events
  </Card>
  <Card
    title="Refresh Token"
    icon="arrows-rotate"
    href="/api-reference/endpoints/auth/refresh"
  >
    Refresh expired tokens
  </Card>
  <Card
    title="Get Player Status"
    icon="user"
    href="/api-reference/endpoints/player/status"
  >
    Retrieve player predictions
  </Card>
</CardGroup>